type Query {
  getAllMembers: [Member]
  getMemberById(id: Int!): Member
}

type Mutation {
  loginMember(id: Int!): Member
  getMemberByIdForMutation(id: Int!): Member
}

type Member {
  id: Int!,
  name: String,
  email: String,
  refresh_token: String,
  member: Int,
}
