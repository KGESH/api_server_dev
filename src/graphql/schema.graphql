type Query {
  getAllUser: [user]
  emailUser(email: String!): user
  authUser: user
  getUserById(id: Int!): user

}

type user {
  id: Int!,
  name: String!,
  email: String!,
  auth: String!,
  qr_list: [String],
  point: Int!
}
